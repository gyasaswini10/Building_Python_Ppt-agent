# Reflection (Agentic_assignment_1.md)

## 1) Where did your agent fail its first attempt?

Write what went wrong (for example: Claude output was not valid JSON, slide bullets count was incorrect, MCP tool parsing failed, etc.) and how you fixed it.

---

## 2) How did MCP prevent you from writing hardcoded scripts?

Explain what you had to *not* hardcode because you relied on MCP tools instead (for example: you didn’t directly build PPT XML by hand; you called `add_slide` repeatedly via MCP; you didn’t assume fixed slide templates in code).

